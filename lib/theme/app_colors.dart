import 'package:flutter/material.dart';

class AppColors {
  // --- Paleta Principal ---
  static const Color primary = Color(0xFF575FFF);
  static const Color accent = Color(0xFFA357FF);
  static const Color tertiary = Color(0xFF57B3FF);

  // --- Fondos y Componentes ---
  static const Color background = Color(0xFFFFFFFF);
  static const Color componentBase = Color(0xFFF4F6F8);

  // --- Texto ---
  static const Color textPrimary = Color(0xFF1E1E1E);
  static const Color textSecondary = Color(0xFF6E6E73);
  static const Color borders = Color(0xFFE0E0E0);

  // --- Semántica ---
  static const Color success = Color(0xFF28A745);
  static const Color error = Color(0xFFDC3545);
  static const Color warning = Color(0xFFFFC107);
}